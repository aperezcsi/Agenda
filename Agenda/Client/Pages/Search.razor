@page "/search"
@using Agenda.Shared.Models;
@inject HttpClient Http
@inject NavigationManager Navigation

<h3>Buscar Contacto</h3>

<!--<EditForm Model="@_contact" OnValidSubmit="Get">
    <div class="form-group">
        <label>Nombre: </label>
        <InputText @bind-Value="_contact._name" />
    </div>
    <div class="form-group">
        <input type="submit" class="btn btn-success" value="Buscar Contacto" />
    </div>
</EditForm>-->

@code {
    /*[Parameter]
    public string name { get; set; }
    private Contact _contact = new();

    private async Task Get()
    {
        _contact = await Task.Run(()=> ValuesController.Get)
        await Http.GetAsync<Contact>("/api/Contact/", _contact);
        Navigation.NavigateTo("/contacts");
    }*/
}
